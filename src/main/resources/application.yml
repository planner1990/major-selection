spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/major_selection
    username: major_user
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

recommendation:
  file-path: /home/hosseini/modemkomak_scraper/clean_results.json
